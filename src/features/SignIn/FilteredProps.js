import React from "react";
import { Formik, Form, Field } from "formik";

export const form = 'form'

// Wrapper for Formik component
// export function FilteredPropsFormContent({ className, ...props }) {
//   return <Formik {...props} />;
// }

// // Form component with customizable props
// export function FilteredPropsForm({ className, ...props }) {
//   return <Form className={className} {...props} />;
// }

// // Input Field with customizable props
// export function FilteredPropsInputField({ className, ...props }) {
//   return <Field className={className} {...props} />;
// }



// import React from "react";

// import { Formik, Form, Field } from "formik";

// export function FilteredPropsFormContent({ className, valid, error, ...props }) {
//   return <Formik className={className} {...props} />;
// }

// export function FilteredPropsForm({ className, valid, error, ...props }) {
//   return <Form className={className} {...props} />;
// }

// export function FilteredPropsInputField({ className, valid, error, ...props }) {
//   return <Field className={className} {...props} />;
// }
